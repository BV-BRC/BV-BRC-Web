// DEPRECATED: p3/layer/p3user is no longer used.
// Auth widgets are bundled into the core layer; this stub remains only to avoid 404s
// if any legacy references exist. It performs no work.
define([], function () {
  try { if (console && console.warn) { console.warn('Deprecated: p3/layer/p3user; auth widgets are in core.'); } } catch (e) {}
  return {};
});
