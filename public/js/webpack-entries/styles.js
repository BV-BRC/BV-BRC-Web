/**
 * Aggregate stylesheet entry to bundle the app's primary styles.
 * Using the top-level p3.css pulls in the rest of the app, theme,
 * and third-party CSS via its @import directives.
 */

import '../p3/resources/p3.css';
