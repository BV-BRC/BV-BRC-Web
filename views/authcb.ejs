<!DOCTYPE html>
<html>
	<head></head>
	<body>
		Login Successful
	<script>
		var message=<%- JSON.stringify({type: "AuthenticationSuccess", userProfile:request.session.userProfile,accessToken: request.session.accessToken}) %>
		if (window.opener || window.parent){
			console.log("Post Parent Message: ", message);
			(window.opener || window.parent).postMessage(message,"*");
		}else{
			window.location="/";
		}
	</script>
	</body>	
</html>
